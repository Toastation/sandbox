
function f(x, y) {
  
}

function setup() {
  width = windowWidth / 2;
  height = windowHeight / 2;
  let canvas = createCanvas(width, height);
  canvas.parent("sketch");
  c1 = color(255, 65, 65)
  c2 = color(65, 65, 255);
}

function draw() {
  background(0);

}